[tool.uv]
package = false

[project]
name = "savings-circle"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "bcrypt>=5.0.0",
    "fastapi[standard]>=0.141.1",
    "PyJWT>=2.10.1",
    "sqlmodel>=0.0.42",
]
